import styled from 'styled-components';

export const CodeContainer = styled.div`
  height: auto;
  width: min(700px, 100vw);
  margin: 0 auto;
  margin-bottom: 30px;
  & > pre {
    width: 100%;
  }
`;
